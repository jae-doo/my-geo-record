package site.jaedoo.mygeorecord.domain.entity;

import org.springframework.data.relational.core.conversion.ObjectPath;

import java.util.Arrays;

public enum DataType {
    NUMBER, FLOAT, STRING, EMAIL, URL;
}
