<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.jaedoo.mygeorecord.mybatis.mapper.FieldValueMapper">
    <select id="findAllStringValues" resultType="string">
        select data
        from spatial_string_field;
    </select>
    <select id="findAllNumberValues" resultType="integer">
        select data
        from spatial_number_field;
    </select>
</mapper>